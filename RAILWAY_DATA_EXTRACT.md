# 📦 Распаковка данных в Railway Volume

Архив уже загружен на Railway: `/tmp/data-backup-20251226-141714.tar.gz` (74MB)

## Шаг 1: Подключение к Railway

Используйте один из способов:

### Вариант A: Через Railway Dashboard
1. Откройте сервис `web` → **Deployments** → последний деплой
2. Нажмите **"View Logs"** или **"Terminal"**
3. Выполните команды ниже

### Вариант B: Через Railway CLI (если поддерживается)
```bash
railway shell --service web
```

## Шаг 2: Проверка Volume

```bash
# Проверка наличия /data
ls -la /data

# Если /data существует, продолжайте
# Если нет - проверьте логи деплоя
```

## Шаг 3: Распаковка данных

```bash
# Перейдите в /data
cd /data

# Распакуйте архив
tar -xzf /tmp/data-backup-20251226-141714.tar.gz

# Удалите архив
rm /tmp/data-backup-20251226-141714.tar.gz

# Проверьте результат
du -sh /data/*
find /data/img -type f | wc -l
find /data/giftimg -type f | wc -l
find /data/log -type f | wc -l
```

## Ожидаемый результат

- `/data/img/` - 235 файлов (~71 MB)
- `/data/giftimg/` - 4 файла (~268 KB)
- `/data/log/` - 243 файла (~28 MB)

## Если /data не существует

1. Проверьте логи деплоя в Railway Dashboard
2. Убедитесь, что Volume `web-volume` подключен к сервису `web`
3. Проверьте, что Mount Path = `/data`
4. Попробуйте еще раз перезапустить сервис через Dashboard

