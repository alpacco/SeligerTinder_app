{
  "name": "tinder-app",
  "version": "1.0.0",
  "description": "Mini-app for Tinder-like functionality",
  "main": "app.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node start-app.js",
    "start:bot": "node start-bot.js",
    "start:all": "node start-all.js",
    "build:prod": "vite build",
    "postinstall": "npm rebuild sqlite3 && node -e \"console.log('🔍 === OpenCV Build Debug Info ==='); console.log('🔍 Python:', require('child_process').execSync('which python3', {encoding: 'utf8'}).trim()); console.log('🔍 Python version:', require('child_process').execSync('python3 --version', {encoding: 'utf8'}).trim()); console.log('🔍 GCC:', require('child_process').execSync('which gcc', {encoding: 'utf8'}).trim()); console.log('🔍 CMake:', require('child_process').execSync('which cmake', {encoding: 'utf8'}).trim()); console.log('🔍 pkg-config:', require('child_process').execSync('which pkg-config', {encoding: 'utf8'}).trim()); console.log('🔍 Checking OpenCV...'); try { console.log('OpenCV version:', require('child_process').execSync('pkg-config --modversion opencv4', {encoding: 'utf8'}).trim()); } catch(e) { console.log('⚠️ pkg-config opencv4 not found'); } try { console.log('OpenCV cflags:', require('child_process').execSync('pkg-config --cflags opencv4', {encoding: 'utf8'}).trim()); } catch(e) { console.log('⚠️ pkg-config opencv4 cflags not found'); } try { console.log('OpenCV libs:', require('child_process').execSync('pkg-config --libs opencv4', {encoding: 'utf8'}).trim()); } catch(e) { console.log('⚠️ pkg-config opencv4 libs not found'); }\" && npm rebuild @u4/opencv4nodejs --verbose 2>&1 | tail -50 || echo '⚠️ OpenCV rebuild failed'",
    "migrate": "node migrations/migrate.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-rate-limit": "^7.1.5",
    "fs": "^0.0.1-security",
    "helmet": "^7.1.0",
    "http-errors": "^2.0.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2",
    "node-telegram-bot-api": "^0.64.0",
    "npm-run-all": "^4.1.5",
    "path": "^0.12.7",
    "sharp": "^0.34.3",
    "pg": "^8.11.3",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "telegraf": "^4.16.3"
  },
  "optionalDependencies": {
    "@u4/opencv4nodejs": "^7.1.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "vite": "^5.0.0"
  },
  "license": "MIT",
  "author": "alpacco"
}
