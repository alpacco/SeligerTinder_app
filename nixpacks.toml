[phases.setup]
nixPkgs = [
  "nodejs_22",
  "npm-9_x",
  "python3",
  "python3Packages.pip",
  "pkg-config",
  "opencv4",
  "gcc",
  "cmake"
]

[variables]
PYTHON = "python3"
npm_config_python = "python3"

[phases.install]
cmds = [
  "export PATH=/nix/store/*/bin:$PATH",
  "export PYTHON=$(which python3) && export npm_config_python=$(which python3)",
  "export NODE_PATH=$(which node | xargs dirname)",
  "echo '🔍 Node.js version:' && node --version",
  "echo '🔍 npm version:' && npm --version",
  "echo '🔍 Python path: $PYTHON'",
  "npm i",
  "echo '🔍 === OpenCV Diagnostics After npm install ==='",
  "echo '🔍 Node.js:' && node --version",
  "echo '🔍 npm:' && npm --version",
  "echo '🔍 Python:' && which python3 && python3 --version",
  "echo '🔍 GCC:' && which gcc && gcc --version | head -1",
  "echo '🔍 CMake:' && which cmake && cmake --version | head -1",
  "echo '🔍 pkg-config:' && which pkg-config",
  "echo '🔍 Checking OpenCV...'",
  "(pkg-config --modversion opencv4 2>&1 || echo '⚠️ pkg-config opencv4 not found')",
  "(pkg-config --cflags opencv4 2>&1 || echo '⚠️ pkg-config opencv4 cflags not found')",
  "(pkg-config --libs opencv4 2>&1 || echo '⚠️ pkg-config opencv4 libs not found')",
  "echo '🔍 === Attempting OpenCV Rebuild ==='",
  "npm rebuild @u4/opencv4nodejs --verbose 2>&1 || echo '⚠️ OpenCV rebuild failed - face detection will be disabled'"
]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "node start-all.js"
