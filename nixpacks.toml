[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "pkg-config", "opencv", "postgresql", "nodejs_20"]

[phases.install]
cmds = [
  # Установка Python зависимостей
  "python3.12 -m venv /app/.venv || python -m venv /app/.venv",
  ". /app/.venv/bin/activate && pip install --upgrade pip",
  ". /app/.venv/bin/activate && pip install -r requirements.txt",
  # Сборка фронтенда (как в другом проекте)
  "echo '========================================'",
  "echo '📦 [BUILD] Начало сборки фронтенда...'",
  "echo '========================================'",
  "echo '📦 [BUILD] Проверка директории frontend...'",
  "ls -la /app/frontend || echo '⚠️ Директория frontend не найдена'",
  "if [ -d '/app/frontend' ]; then",
  "  cd /app/frontend && echo '📦 [BUILD] Текущая директория:' && pwd",
  "  cd /app/frontend && echo '📦 [BUILD] Установка npm зависимостей...' && npm install",
  "  cd /app/frontend && echo '📦 [BUILD] Запуск сборки (npm run build)...' && npm run build",
  "  echo '📦 [BUILD] Проверка результатов сборки...'",
  "  ls -la /app/public/js/ || echo '⚠️ Директория public/js не найдена'",
  "  ls -la /app/public/css/ || echo '⚠️ Директория public/css не найдена'",
  "  test -f /app/public/hash-map.json && echo '✅ hash-map.json создан' || echo '⚠️ hash-map.json НЕ создан'",
  "else",
  "  echo '⚠️ Директория frontend не найдена, пропускаем сборку фронтенда'",
  "fi",
  "echo '========================================'",
  "echo '📦 [BUILD] Сборка фронтенда завершена'",
  "echo '========================================'"
]

# Railway должен использовать Procfile для запуска процессов
# НЕ указываем [start] здесь, чтобы Railway использовал Procfile
# Procfile содержит:
#   web: cd python_backend && python3.12 -m uvicorn main:app --host 0.0.0.0 --port $PORT
