[phases.setup]
nixPkgs = [
  "nodejs_22",
  "npm-9_x",
  "python3",
  "python3Packages.pip",
  "pkg-config",
  "opencv4",
  "gcc",
  "cmake"
]

[variables]
PYTHON = "/nix/store/*/bin/python3"
npm_config_python = "/nix/store/*/bin/python3"

[phases.install]
cmds = ["npm i"]

[phases.build]
cmds = ["npm rebuild @u4/opencv4nodejs || echo 'OpenCV rebuild skipped - will continue without face detection'"]

[start]
cmd = "node start-all.js"
