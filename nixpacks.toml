[phases.setup]
nixPkgs = [
  "nodejs_22",
  "npm-9_x",
  "python3",
  "python3Packages.pip",
  "pkg-config",
  "opencv4",
  "gcc",
  "cmake"
]

[variables]
PYTHON = "python3"
npm_config_python = "python3"

[phases.install]
cmds = [
  "export PATH=/nix/store/*/bin:$PATH && export PYTHON=$(which python3) && export npm_config_python=$(which python3) && export NODE_PATH=$(which node | xargs dirname) && OPENCV_PC=$(find /nix/store -name 'opencv4.pc' -o -name 'opencv.pc' 2>/dev/null | head -1) && if [ -n \"$OPENCV_PC\" ]; then export PKG_CONFIG_PATH=$(dirname \"$OPENCV_PC\"):$PKG_CONFIG_PATH && echo '🔍 Found OpenCV .pc at: $OPENCV_PC' && echo '🔍 PKG_CONFIG_PATH set to: $PKG_CONFIG_PATH'; else echo '⚠️ OpenCV .pc file not found'; fi",
  "echo '🔍 Node.js version:' && node --version",
  "echo '🔍 npm version:' && npm --version",
  "echo '🔍 Python path:' && which python3 && python3 --version",
  "echo '🔍 PKG_CONFIG_PATH:' && echo $PKG_CONFIG_PATH",
  "echo '🔍 Searching for OpenCV .pc files...' && find /nix/store -name 'opencv*.pc' 2>/dev/null | head -3 || echo '⚠️ No OpenCV .pc files found'",
  "npm i",
  "echo '🔍 === OpenCV Diagnostics After npm install ==='",
  "OPENCV_PC=$(find /nix/store -name 'opencv4.pc' -o -name 'opencv.pc' 2>/dev/null | head -1) && if [ -n \"$OPENCV_PC\" ]; then export PKG_CONFIG_PATH=$(dirname \"$OPENCV_PC\"):$PKG_CONFIG_PATH; fi",
  "echo '🔍 Node.js:' && node --version",
  "echo '🔍 npm:' && npm --version",
  "echo '🔍 Python:' && which python3 && python3 --version",
  "echo '🔍 GCC:' && which gcc && gcc --version | head -1",
  "echo '🔍 CMake:' && which cmake && cmake --version | head -1",
  "echo '🔍 pkg-config:' && which pkg-config",
  "echo '🔍 PKG_CONFIG_PATH:' && echo $PKG_CONFIG_PATH",
  "echo '🔍 Checking OpenCV with pkg-config...'",
  "(pkg-config --modversion opencv4 2>&1 || pkg-config --modversion opencv 2>&1 || echo '⚠️ pkg-config opencv4/opencv not found')",
  "(pkg-config --cflags opencv4 2>&1 || pkg-config --cflags opencv 2>&1 || echo '⚠️ pkg-config opencv4/opencv cflags not found')",
  "(pkg-config --libs opencv4 2>&1 || pkg-config --libs opencv 2>&1 || echo '⚠️ pkg-config opencv4/opencv libs not found')",
  "echo '🔍 === Attempting OpenCV Rebuild ==='",
  "OPENCV_PC=$(find /nix/store -name 'opencv4.pc' -o -name 'opencv.pc' 2>/dev/null | head -1) && if [ -n \"$OPENCV_PC\" ]; then export PKG_CONFIG_PATH=$(dirname \"$OPENCV_PC\"):$PKG_CONFIG_PATH; fi && npm rebuild @u4/opencv4nodejs --verbose 2>&1 || echo '⚠️ OpenCV rebuild failed - face detection will be disabled'"
]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "node start-all.js"
