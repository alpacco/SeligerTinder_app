# SeligerTinder - Telegram Mini App

Tinder-подобное приложение для Telegram с веб-интерфейсом и ботом.

## 🚀 Технологии

- **Backend**: Node.js, Express
- **Bot**: Telegraf
- **Database**: SQLite
- **Frontend**: EJS, Vite
- **API**: Google Vision API (опционально)

## 📋 Требования

- Node.js >= 18.0.0
- npm >= 9.0.0

## 🔧 Установка

```bash
npm install
```

## 🏃 Запуск локально

```bash
# Запуск приложения и бота одновременно
npm run start:all

# Или отдельно:
npm start          # Только веб-сервер
npm run start:bot  # Только бот
```

## 🌐 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
WEB_APP_URL=https://your-domain.com

# Server
PORT=8080
NODE_ENV=production
LOCAL=false

# Database (выберите один вариант)
# Вариант 1: PostgreSQL (Railway автоматически предоставляет)
DATABASE_URL=postgresql://user:password@host:port/database
# Или
USE_POSTGRES=true
PGHOST=localhost
PGPORT=5432
PGDATABASE=railway
PGUSER=postgres
PGPASSWORD=your_password

# Вариант 2: SQLite (по умолчанию, если не указан PostgreSQL)
# Ничего не указывайте, будет использоваться SQLite

# Google Vision API (опционально)
# Ключ должен быть в файле Google Vision/vision-key.json
```

### База данных

Проект поддерживает **SQLite** и **PostgreSQL**:
- **SQLite** используется по умолчанию (если не указан PostgreSQL)
- **PostgreSQL** используется если установлена `USE_POSTGRES=true` или `DATABASE_URL`

Для миграции на PostgreSQL см. [POSTGRESQL_MIGRATION.md](./POSTGRESQL_MIGRATION.md)

## 📦 Деплой на Railway

### 1. Подготовка GitHub репозитория

```bash
# Проверьте текущие remotes
git remote -v

# Добавьте GitHub remote (замените на ваш репозиторий)
git remote add github https://github.com/yourusername/sta-black-master.git

# Или измените существующий remote
git remote set-url origin https://github.com/yourusername/sta-black-master.git

# Закоммитьте изменения
git add .
git commit -m "Подготовка к деплою на Railway"

# Отправьте в GitHub
git push github master
# или
git push origin master
```

### 2. Настройка Railway

1. Зайдите на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js проект

### 3. Настройка переменных окружения в Railway

В настройках проекта Railway добавьте следующие переменные:

- `BOT_TOKEN` - токен вашего Telegram бота
- `WEB_APP_URL` - URL вашего приложения (Railway предоставит домен)
- `PORT` - порт (Railway автоматически установит через переменную `PORT`)
- `NODE_ENV=production`
- `LOCAL=false`

### 4. Настройка базы данных

#### Вариант A: PostgreSQL (рекомендуется для Railway)

1. В Railway Dashboard добавьте PostgreSQL сервис
2. Railway автоматически установит переменную `DATABASE_URL`
3. Выполните миграции:
   ```bash
   npm run migrate
   ```

#### Вариант B: SQLite (для локальной разработки)

1. В Railway Dashboard добавьте Volume
2. Смонтируйте его в `/data`
3. Это позволит сохранять базу данных и изображения между деплоями

**Важно**: Пути `/data/img/`, `/data/giftimg/`, `/data/log/` должны быть доступны через Volume (даже при использовании PostgreSQL).

### 5. Google Vision API (опционально)

Если используете Google Vision API:

1. Создайте сервисный аккаунт в Google Cloud Console
2. Скачайте JSON ключ
3. В Railway добавьте переменную окружения `GOOGLE_APPLICATION_CREDENTIALS` со значением пути к ключу
4. Или загрузите файл ключа через Railway Secrets

## 📁 Структура проекта

```
├── app.js              # Основной Express сервер
├── bot.js              # Telegram бот
├── db.js               # Инициализация базы данных
├── start-app.js        # Запуск веб-сервера
├── start-bot.js        # Запуск бота
├── start-all.js        # Запуск приложения и бота
├── routes/             # API маршруты
├── views/              # EJS шаблоны
├── public/             # Статические файлы
└── Google Vision/      # Google Vision API ключ (не в git)
```

## 🔐 Безопасность

- Не коммитьте `.env` файлы
- Не коммитьте `Google Vision/vision-key.json`
- Не коммитьте базы данных (`*.db`, `*.sqlite`)
- Используйте Railway Secrets для чувствительных данных

## 📝 API Endpoints

- `/api/users` - управление пользователями
- `/api/likes` - лайки
- `/api/matches` - совпадения
- `/api/photos` - загрузка фотографий
- `/api/gifts` - подарки
- `/api/pro` - PRO подписка
- `/api/stats` - статистика

## 🤖 Команды бота

- `/start` - начать работу с ботом
- `/delete_user` - удалить профиль
- `/clear_photos` - удалить все фото
- `/stats` - статистика (только для админа)
- `/masssend` - массовая рассылка (только для админа)
- `/addbadge` - установить бейдж (только для админа)
- `/grantpro` - выдать PRO подписку (только для админа)

## 🐛 Troubleshooting

### Проблемы с базой данных на Railway

Убедитесь, что Volume смонтирован в `/data` и имеет права на запись.

### Бот не запускается

Проверьте:
- Правильность `BOT_TOKEN`
- Доступность интернета для бота
- Логи в Railway Dashboard

### Порт не определен

Railway автоматически устанавливает переменную `PORT`. Убедитесь, что `start-app.js` использует `process.env.PORT`.

## 📄 Лицензия

MIT

## 👤 Автор

alpacco

