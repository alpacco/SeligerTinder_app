# 🚀 Telegram WebApp Cache Management Best Practices

## 📋 Обзор проблемы

Telegram WebApp агрессивно кэширует JavaScript файлы, что приводит к тому, что изменения в коде не отображаются пользователям даже после деплоя на production.

## 🎯 Стратегии обхода кэша

### 1. **Версионирование приложения (Рекомендуется)**

#### В `webapp/src/js/modules/app.js`:
```javascript
// Обновляем версию при каждом изменении
const VERSION = '2025-01-15-feature-name'; // Формат: YYYY-MM-DD-description

// Принудительная перезагрузка при изменении версии
(function forceReloadIfVersionChanged() {
    try {
        const HTML_VERSION_KEY = 'sp_cache_version_html';
        const prev = localStorage.getItem(HTML_VERSION_KEY);
        if (prev !== VERSION) {
            localStorage.setItem(HTML_VERSION_KEY, VERSION);
            const url = window.location.pathname + '?v=' + VERSION;
            console.log('🔄 [Cache] ПРИНУДИТЕЛЬНАЯ ПЕРЕЗАГРУЗКА:', url);
            window.location.replace(url);
        }
    } catch (e) {
        console.warn('⚠️ Cache cleanup skipped:', e);
    }
})();
```

#### В `webapp/index.html`:
```html
<!-- Добавляем версию ко всем скриптам -->
<script src="src/js/modules/api-client.js?v=2025-01-15-feature-name"></script>
<script src="src/js/modules/auth-manager.js?v=2025-01-15-feature-name"></script>
```

### 2. **Динамическая загрузка критических модулей**

#### Для модулей, которые часто изменяются:
```html
<script>
// Динамическая загрузка с уникальным timestamp
(function() {
    const script = document.createElement('script');
    const timestamp = Date.now();
    script.src = `src/js/modules/critical-module.js?t=${timestamp}`;
    script.onload = function() {
        console.log('✅ Critical module загружен с timestamp:', timestamp);
    };
    script.onerror = function() {
        console.error('❌ Ошибка загрузки critical module');
    };
    document.body.appendChild(script);
})();
</script>
```

### 3. **Агрессивный кэш-бастинг для критических файлов**

#### В критических модулях добавляем:
```javascript
// В начале файла
const SCRIPT_VERSION = '2025-01-15-critical-fix-v3';
console.log('🔄 FORCE CACHE CLEAR - Script загружен заново!');
console.log('🔄 Время загрузки:', new Date().toISOString());
console.log('🔄 Уникальный ID:', Math.random().toString(36).substr(2, 9));
```

## 🛠️ Практические рекомендации

### ✅ Что ДЕЛАТЬ:

1. **Всегда обновляйте версию** при изменении любого JS файла
2. **Используйте осмысленные версии**: `2025-01-15-payment-fix-v2`
3. **Добавляйте версии к критическим скриптам** в HTML
4. **Используйте динамическую загрузку** для часто изменяемых модулей
5. **Логируйте загрузку модулей** для отладки

### ❌ Что НЕ ДЕЛАТЬ:

1. **Не полагайтесь только на git commit** - Telegram не знает о git
2. **Не используйте одинаковые версии** для разных изменений
3. **Не забывайте обновлять версии** в HTML при изменении JS
4. **Не создавайте заглушки** которые перехватывают настоящие модули

## 🔧 Чек-лист при изменении кода

### Перед коммитом:
- [ ] Обновлена версия в `app.js`
- [ ] Обновлены версии скриптов в `index.html`
- [ ] Добавлены логи загрузки для критических модулей
- [ ] Убраны заглушки и временные решения

### После деплоя:
- [ ] Проверить логи в браузере на наличие "FORCE CACHE CLEAR"
- [ ] Убедиться что модули загружаются с новыми версиями
- [ ] Протестировать функциональность в Telegram

## 🚨 Экстренные меры

### Если кэш не обновляется:

1. **Увеличьте версию** в `app.js`
2. **Добавьте timestamp** к критическим скриптам
3. **Используйте динамическую загрузку** для проблемных модулей
4. **Добавьте агрессивные логи** для отслеживания загрузки

### Пример экстренного кэш-бастинга:
```javascript
// В проблемном модуле
const EMERGENCY_VERSION = '2025-01-15-emergency-fix-' + Date.now();
console.log('🚨 EMERGENCY CACHE CLEAR:', EMERGENCY_VERSION);
```

## 📊 Мониторинг кэша

### Логи для отслеживания:
```javascript
// В каждом модуле
console.log('🔄 [ModuleName] Загружен версия:', VERSION);
console.log('🔄 [ModuleName] Время загрузки:', new Date().toISOString());
console.log('🔄 [ModuleName] Уникальный ID:', Math.random().toString(36).substr(2, 9));
```

### Проверка в браузере:
1. Откройте DevTools → Console
2. Найдите логи "FORCE CACHE CLEAR"
3. Убедитесь что модули загружаются с новыми версиями
4. Проверьте что нет ошибок загрузки

## 🎯 Специальные случаи

### Для PaymentManager:
```html
<!-- Динамическая загрузка с timestamp -->
<script>
(function() {
    const script = document.createElement('script');
    const timestamp = Date.now();
    script.src = `src/js/modules/payment-manager.js?t=${timestamp}`;
    script.onload = function() {
        console.log('✅ PaymentManager загружен динамически с timestamp:', timestamp);
    };
    document.body.appendChild(script);
})();
</script>
```

### Для критических CSS:
```html
<link rel="stylesheet" href="src/css/modules/critical.module.css?v=2025-01-15-feature-name">
```

## 📝 Шаблон версионирования

### Формат версии:
```
YYYY-MM-DD-description-vNUMBER
```

### Примеры:
- `2025-01-15-payment-fix-v1`
- `2025-01-15-timer-restore-v2`
- `2025-01-15-cache-emergency-v3`

## 🔄 Автоматизация

### Git hook для автоматического обновления версии:
```bash
#!/bin/sh
# .git/hooks/pre-commit
# Автоматически обновляет версию в app.js при коммите

VERSION=$(date +"%Y-%m-%d")-$(git log -1 --pretty=%s | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/-/g' | sed 's/--*/-/g' | sed 's/^-\|-$//g')
sed -i "s/const VERSION = '.*'/const VERSION = '$VERSION'/" webapp/src/js/modules/app.js
```

## 🎯 Заключение

Правильное управление кэшем критически важно для Telegram WebApp. Используйте эти практики для обеспечения того, чтобы пользователи всегда видят последние изменения.

**Помните**: Telegram кэширует агрессивно, поэтому всегда планируйте обход кэша заранее!
