# Резюме миграции на PostgreSQL

## ✅ Что сделано

### 1. Поддержка PostgreSQL
- ✅ Добавлен модуль `db-pg.js` для работы с PostgreSQL
- ✅ Обновлен `db.js` для автоматического переключения между SQLite и PostgreSQL
- ✅ Обновлены утилиты `utils/db.js` для поддержки обеих БД

### 2. Система миграций
- ✅ Создан скрипт миграций `migrations/migrate.js`
- ✅ Создана начальная миграция `migrations/001_initial_schema.sql`
- ✅ Добавлена таблица `migrations` для отслеживания выполненных миграций

### 3. Обновления кода
- ✅ Обновлен `app.js` для работы с обеими БД
- ✅ Обновлен `routes/likes.js` как пример адаптации routes
- ✅ Все утилиты автоматически адаптируются к типу БД

### 4. Документация
- ✅ Создан `POSTGRESQL_MIGRATION.md` с подробными инструкциями
- ✅ Обновлен `README.md` с информацией о БД

## 🔄 Как переключиться на PostgreSQL

### В Railway (автоматически)
1. Добавьте PostgreSQL сервис в Railway
2. Railway автоматически установит `DATABASE_URL`
3. Приложение автоматически переключится на PostgreSQL

### Вручную
1. Установите переменную окружения:
   ```env
   USE_POSTGRES=true
   # или
   DATABASE_URL=postgresql://user:password@host:port/database
   ```

2. Выполните миграции:
   ```bash
   npm run migrate
   ```

3. Перезапустите приложение

## 📋 Что нужно сделать дальше

### 1. Миграция данных из SQLite
Когда будете готовы, нужно будет:
- Экспортировать данные из SQLite
- Адаптировать SQL для PostgreSQL
- Импортировать в PostgreSQL

Скрипт для миграции данных будет создан позже.

### 2. Обновление остальных routes
Некоторые routes могут требовать адаптации SQL запросов:
- Замена `?` на `$1, $2, $3...` для PostgreSQL
- Добавление кавычек к идентификаторам: `userId` → `"userId"`

Пример адаптации в `routes/likes.js`:
```javascript
const sql = USE_POSTGRES 
  ? "SELECT likes, matches FROM users WHERE \"userId\" = $1"
  : "SELECT likes, matches FROM users WHERE userId = ?";
```

### 3. Тестирование
- Протестировать все API endpoints с PostgreSQL
- Проверить работу транзакций
- Убедиться, что все запросы работают корректно

## 🔍 Проверка работы

После настройки PostgreSQL проверьте:

```bash
# Проверка подключения
node -e "const {pool} = require('./db'); pool.query('SELECT NOW()').then(r => console.log('OK:', r.rows[0])).catch(e => console.error('ERROR:', e));"

# Выполнение миграций
npm run migrate
```

## 📝 Примечания

- Все изменения обратно совместимы
- Можно переключаться между SQLite и PostgreSQL через переменные окружения
- Существующий код продолжит работать без изменений
- Миграции выполняются автоматически при наличии `DATABASE_URL`

