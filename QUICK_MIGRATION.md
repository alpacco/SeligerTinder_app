# ⚡ Быстрая миграция данных в Railway Volume

## 📋 Что нужно сделать

1. **Создать Volume в Railway** (1 минута)
2. **Запустить скрипт миграции** (5-10 минут)

## 🚀 Пошаговая инструкция

### Шаг 1: Создание Volume

1. Откройте [Railway Dashboard](https://railway.app)
2. Ваш проект → **Settings** → **Volumes**
3. **Create Volume**:
   - Name: `data-volume`
   - Mount Path: `/data`
   - Size: `10 GB`

### Шаг 2: Автоматическая миграция

```bash
cd /Users/dmitryalexeev/sta-black-master

# Установите Railway CLI (если нужно)
brew install railway

# Авторизуйтесь
railway login

# Подключитесь к проекту
railway link

# Запустите миграцию
export RAILWAY_SERVICE=web  # Замените на имя вашего сервиса
./scripts/upload_to_railway.sh
```

Готово! Скрипт автоматически:
- ✅ Создаст архив (71MB изображений + 28MB логов)
- ✅ Загрузит на Railway
- ✅ Распакует в `/data`
- ✅ Проверит результат

## 📊 Что будет мигрировано

- ✅ **235 файлов** фотографий пользователей (`/data/img/`)
- ✅ Изображения подарков (`/data/giftimg/`)
- ✅ Логи приложения (`/data/log/`)

**Размер данных**: ~100 MB

## ⚠️ Если скрипт не работает

См. подробную инструкцию в `MIGRATION_GUIDE.md` или `doc/RAILWAY_DATA_MIGRATION.md`

## ✅ Проверка после миграции

```bash
railway run --service web --command "du -sh /data/* && find /data/img -type f | wc -l"
```

Должно показать размер директорий и количество файлов.

