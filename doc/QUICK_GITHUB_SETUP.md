# Быстрая настройка GitHub

## ✅ У вас уже есть SSH ключ!

Ваш SSH ключ:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIING8IugX94OJsez5xiYGwfK7lZadviu9qmkdV61BroX dmitryalexeev@MacBook-Air.local
```

## 📋 Пошаговая инструкция

### 1. Добавьте SSH ключ в GitHub

1. Откройте https://github.com/settings/keys
2. Нажмите **"New SSH key"** (зеленая кнопка)
3. **Title**: `MacBook Air` (или любое название)
4. **Key**: Вставьте ключ выше (весь текст начиная с `ssh-ed25519...`)
5. Нажмите **"Add SSH key"**

### 2. Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. **Repository name**: `SeligerTinder_app`
3. Выберите **Private** (рекомендуется)
4. **НЕ** ставьте галочки на README, .gitignore, license
5. Нажмите **"Create repository"**

### 3. Добавьте GitHub remote и отправьте код

После создания репозитория выполните команды (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
# Добавить GitHub remote
git remote add github git@github.com:YOUR_USERNAME/SeligerTinder_app.git

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Добавлена поддержка PostgreSQL, улучшения backend и подготовка к деплою на Railway"

# Отправить в GitHub
git push github master
```

## 🔍 Проверка

Проверьте, что всё работает:

```bash
# Проверка SSH подключения
ssh -T git@github.com

# Должно быть: "Hi YOUR_USERNAME! You've successfully authenticated..."
```

## 📝 Примечание про Deploy Keys

**Deploy Keys** (которые вы видите в настройках репозитория) - это для **серверов** (например Railway), а не для вашего компьютера.

Для вашего компьютера используйте **SSH ключ пользователя** (который мы только что добавили).

Railway может подключиться к GitHub через:
- GitHub App (рекомендуется, автоматически)
- Personal Access Token
- Deploy Key (если нужно)

