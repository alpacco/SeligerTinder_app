# 🔐 Переменные окружения (Environment Variables)

## Обязательные переменные

Эти переменные **обязательны** для работы приложения:

```bash
# База данных
DATABASE_URL=postgresql://user:password@host:port/database
# ИЛИ отдельные переменные:
PGHOST=localhost
PGPORT=5432
PGDATABASE=railway
PGUSER=postgres
PGPASSWORD=your-password

# Telegram Bot
BOT_TOKEN=your-telegram-bot-token

# Web App URL
WEB_APP_URL=https://your-app.up.railway.app
```

## Рекомендуемые переменные (Безопасность)

```bash
# Admin авторизация (выберите один способ)
ADMIN_TELEGRAM_IDS=123456789,987654321  # Telegram ID администраторов (через запятую)
# ИЛИ
ADMIN_TOKEN=your-secret-admin-token  # Bearer token для admin endpoints
```

## Опциональные переменные (Настройки)

### Безопасность и Rate Limiting

```bash
# Rate Limiting
RATE_LIMIT_PER_HOUR=1000      # Лимит запросов в час (по умолчанию: 1000)
RATE_LIMIT_PER_MINUTE=100     # Лимит запросов в минуту (по умолчанию: 100)

# File Upload
MAX_FILE_SIZE_MB=10           # Максимальный размер файла в MB (по умолчанию: 10)
MAX_IMAGE_DIMENSION=10000     # Максимальный размер изображения в пикселях (по умолчанию: 10000)

# HTTP Timeouts
HTTP_TIMEOUT=30               # Таймаут для HTTP запросов в секундах (по умолчанию: 30)
HTTP_MAX_REDIRECTS=5          # Максимальное количество редиректов (по умолчанию: 5)
```

### Пути и директории

```bash
# Пути для хранения данных (по умолчанию: /data/img и /data/log)
IMAGES_DIR=/data/img          # Директория для изображений пользователей
LOG_DIR=/data/log             # Директория для логов
```

### CORS (Cross-Origin Resource Sharing)

```bash
# Дополнительные разрешенные origins (через запятую)
CORS_ORIGINS=https://example.com,https://another-domain.com
```

### Логирование

```bash
# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Логировать ли чувствительные данные (только для разработки!)
LOG_SENSITIVE=false           # true/false (по умолчанию: false)
```

### Environment и Debug

```bash
# Environment (production, development, staging)
ENVIRONMENT=production

# Debug mode (автоматически отключается в production)
DEBUG=false                   # true/false (по умолчанию: false)

# Локальная разработка
LOCAL=false                   # true/false (по умолчанию: false)
```

### Порт сервера

```bash
# Порт сервера (Railway устанавливает автоматически)
PORT=8080                     # По умолчанию: 8080
```

### Дополнительные настройки

```bash
# Chat ID для разработки (опционально)
DEV_CHAT_ID=123456789         # 0 = отключено (по умолчанию: 0)
```

## Полный список переменных для Railway

### Минимальная конфигурация (обязательно):

```bash
DATABASE_URL=postgresql://...
BOT_TOKEN=...
WEB_APP_URL=https://...
ADMIN_TELEGRAM_IDS=123456789
```

### Рекомендуемая конфигурация (безопасность):

```bash
DATABASE_URL=postgresql://...
BOT_TOKEN=...
WEB_APP_URL=https://...
ADMIN_TELEGRAM_IDS=123456789
RATE_LIMIT_PER_HOUR=1000
RATE_LIMIT_PER_MINUTE=100
MAX_FILE_SIZE_MB=10
ENVIRONMENT=production
LOG_LEVEL=INFO
```

### Полная конфигурация (все настройки):

```bash
# Обязательные
DATABASE_URL=postgresql://...
BOT_TOKEN=...
WEB_APP_URL=https://...

# Безопасность
ADMIN_TELEGRAM_IDS=123456789,987654321

# Rate Limiting
RATE_LIMIT_PER_HOUR=1000
RATE_LIMIT_PER_MINUTE=100

# File Upload
MAX_FILE_SIZE_MB=10
MAX_IMAGE_DIMENSION=10000

# HTTP
HTTP_TIMEOUT=30
HTTP_MAX_REDIRECTS=5

# Пути
IMAGES_DIR=/data/img
LOG_DIR=/data/log

# CORS
CORS_ORIGINS=https://example.com

# Логирование
LOG_LEVEL=INFO
LOG_SENSITIVE=false

# Environment
ENVIRONMENT=production
DEBUG=false
LOCAL=false

# Порт
PORT=8080
```

## Проверка конфигурации

При запуске приложения проверяется конфигурация. Если что-то не настроено, вы увидите предупреждения:

```
⚠️ ВНИМАНИЕ: BOT_TOKEN не установлен! Бот не будет работать!
⚠️ ВНИМАНИЕ: WEB_APP_URL не установлен!
⚠️ ОШИБКИ КОНФИГУРАЦИИ:
  - BOT_TOKEN не установлен
  - WEB_APP_URL не установлен
```

## Безопасность

### ✅ Что уже защищено:

1. **Все секреты** в переменных окружения
2. **Нет хардкода** токенов, паролей, ключей
3. **Валидация** всех входных данных
4. **Безопасное логирование** (без чувствительных данных)
5. **Настраиваемые лимиты** для защиты от злоупотреблений

### ⚠️ Важно:

- **Никогда** не коммитьте `.env` файлы в Git
- **Никогда** не логируйте токены, пароли, ключи
- **Всегда** используйте переменные окружения для секретов
- **Проверяйте** конфигурацию при деплое

## Пример настройки в Railway

1. Откройте проект в Railway
2. Перейдите в **Variables**
3. Добавьте все обязательные переменные:
   - `DATABASE_URL`
   - `BOT_TOKEN`
   - `WEB_APP_URL`
   - `ADMIN_TELEGRAM_IDS`
4. (Опционально) Добавьте дополнительные настройки
5. Сохраните и перезапустите приложение

## Проверка после деплоя

После деплоя проверьте логи Railway. Вы должны увидеть:

```
✅ Admin авторизация настроена через Telegram ID (1 администратор(ов))
📊 Environment: production
📊 Debug mode: False
📊 Rate limits: 1000/hour, 100/minute
📊 Max file size: 10 MB
✅ Backend server initialized
```

Если видите предупреждения - добавьте недостающие переменные!

