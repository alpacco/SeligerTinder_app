# SeligerTinder

Telegram Mini App для знакомств в ЖК Селигер Сити.

## 🚀 Технологии

- **Backend**: Python 3.11, FastAPI
- **Bot**: python-telegram-bot
- **Database**: PostgreSQL (Railway)
- **Image Processing**: OpenCV (opencv-python-headless)
- **Frontend**: Vanilla JavaScript, EJS templates

## 📋 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env`:
```env
BOT_TOKEN=your_telegram_bot_token
WEB_APP_URL=https://your-domain.com
DATABASE_URL=postgresql://user:password@host:port/database
PORT=8080
```

3. Запустите приложение:
```bash
python python_backend/start_all.py
```

## 🚢 Деплой на Railway

1. Подключите репозиторий к Railway
2. Railway автоматически определит Python проект
3. Установите переменные окружения в Railway Dashboard
4. Приложение запустится автоматически

## 📁 Структура проекта

```
├── python_backend/      # Python бэкенд
│   ├── main.py         # FastAPI сервер
│   ├── bot.py          # Telegram бот
│   ├── database.py     # Работа с БД
│   ├── routes/         # API маршруты
│   └── ...
├── public/             # Статические файлы (CSS, JS, изображения)
├── views/              # EJS шаблоны
├── requirements.txt    # Python зависимости
└── _archive/           # Архив старого Node.js бэкенда
```

## 📝 API Endpoints

- `/api/users` - управление пользователями
- `/api/likes` - лайки
- `/api/matches` - совпадения
- `/api/photos` - загрузка фотографий
- `/api/pro` - PRO подписка
- `/api/stats` - статистика
- `/api/admin` - административные функции
- `/api/goals` - цели пользователей
- `/api/push` - push уведомления

## 🤖 Команды бота

- `/start` - начать работу с ботом
- `/grantpro` - выдать PRO подписку (только для админа)
- `/addbadge` - установить бейдж (только для админа)
- `/stats` - статистика (только для админа)

## 📚 Документация

- `PYTHON_README.md` - подробная документация Python бэкенда
- `FRONTEND_BACKEND_COMPATIBILITY.md` - совместимость фронтенда и бэкенда
- `MIGRATION_CHECK.md` - проверка миграции функций

## 🔧 Переменные окружения

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
WEB_APP_URL=https://your-domain.com

# Database
DATABASE_URL=postgresql://user:password@host:port/database

# Server
PORT=8080
```

## 📊 База данных

Используется PostgreSQL на Railway. Таблицы:
- `users` - пользователи
- `visits` - посещения профилей
- `dislikes` - дизлайки
- `super_likes` - суперлайки
- `badge_requests` - запросы на бейджи

## 🎯 Особенности

- ✅ OpenCV для детекции лиц на фотографиях
- ✅ PRO подписка с расширенными функциями
- ✅ Система бейджей (L, P, S)
- ✅ Push уведомления через Telegram
- ✅ Статистика и аналитика

## 📝 Лицензия

Private project
