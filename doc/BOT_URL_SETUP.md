# 🔗 Настройка URL приложения в боте

## Проблема

Если вы обновили URL в BotFather, но бот все еще открывает старое приложение, это происходит потому что:

1. **Кнопки в боте** (InlineKeyboardButton с WebAppInfo) используют переменную окружения `WEB_APP_URL`
2. **Кнопка меню бота** (Menu Button) может быть установлена через BotFather ИЛИ программно

## Решение

### Вариант 1: Обновить переменную окружения (РЕКОМЕНДУЕТСЯ)

1. Откройте Railway Dashboard
2. Перейдите в ваш сервис
3. Откройте вкладку **Variables**
4. Найдите переменную `WEB_APP_URL`
5. Обновите значение на новый URL: `https://web-production-4bcda.up.railway.app`
6. Сохраните изменения
7. **Перезапустите сервис** (Railway автоматически перезапустит после изменения переменных)

### Вариант 2: Установить через BotFather

1. Откройте BotFather в Telegram
2. Отправьте команду `/setmenubutton`
3. Выберите вашего бота
4. Отправьте новый URL: `https://web-production-4bcda.up.railway.app`

**Примечание**: Этот способ устанавливает только кнопку меню бота. Кнопки внутри бота (в команде /start, в меню) все равно будут использовать `WEB_APP_URL` из переменных окружения.

### Вариант 3: Программная установка (АВТОМАТИЧЕСКИ)

В коде бота добавлена функция `set_menu_button_url()`, которая автоматически устанавливает menu button URL при запуске бота, используя значение из `WEB_APP_URL`.

Это означает, что после обновления переменной окружения `WEB_APP_URL` и перезапуска сервиса, menu button URL будет автоматически обновлен.

## Проверка

После обновления:

1. **Проверьте логи Railway** - должны увидеть:
   ```
   ✅ [BOT] Menu button URL успешно установлен: https://web-production-4bcda.up.railway.app
   ```

2. **Проверьте в Telegram**:
   - Откройте бота
   - Нажмите кнопку меню (три линии внизу)
   - Кнопка "Открыть приложение" должна открывать новый URL

3. **Проверьте команду /start**:
   - Отправьте `/start` боту
   - Нажмите кнопку "✨Открыть Seliger Tinder✨"
   - Должно открыться новое приложение

## Важно

- **Всегда обновляйте переменную окружения `WEB_APP_URL`** - это основной источник URL для всех кнопок WebApp в боте
- **После изменения переменных окружения обязательно перезапустите сервис**
- Menu button URL устанавливается автоматически при запуске бота, если `WEB_APP_URL` установлен

## Текущая конфигурация

- **WEB_APP_URL** (из переменных окружения): Используется для всех кнопок WebApp в боте
- **Menu Button URL** (из BotFather или программно): Используется для кнопки меню бота

Оба должны указывать на один и тот же URL для консистентности.

