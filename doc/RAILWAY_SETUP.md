# Настройка Railway

## ✅ Домен Railway

Домен `web-production-4bcda.up.railway.app` - это **нормальный домен Railway по умолчанию**. 

### Варианты:

1. **Использовать как есть** (рекомендуется для начала)
   - Домен работает сразу
   - HTTPS настроен автоматически
   - Можно использовать для тестирования

2. **Настроить кастомный домен** (опционально)
   - В Railway Dashboard → Settings → Domains
   - Добавьте свой домен (например, `seligertinder.ru`)
   - Настройте DNS записи (Railway покажет инструкции)

## 🔧 Настройка переменных окружения в Railway

В Railway Dashboard → Variables добавьте:

### Обязательные:
```env
BOT_TOKEN=your_telegram_bot_token
WEB_APP_URL=https://web-production-4bcda.up.railway.app
NODE_ENV=production
PORT=8080
```

### Для PostgreSQL (если используете):
```env
DATABASE_URL=postgresql://... (Railway автоматически добавит при подключении PostgreSQL)
# или
USE_POSTGRES=true
```

### Опциональные:
```env
LOCAL=false
SERVER_DOMAIN=https://web-production-4bcda.up.railway.app
```

## 📝 Важно

1. **WEB_APP_URL** должен быть полным URL с `https://`
2. Railway автоматически предоставляет переменную `PORT` - не переопределяйте её
3. Если добавите PostgreSQL, Railway автоматически добавит `DATABASE_URL`

## 🔄 После настройки

1. Railway автоматически перезапустит приложение
2. Проверьте логи в Railway Dashboard
3. Откройте ваш домен: `https://web-production-4bcda.up.railway.app`

## 🌐 Кастомный домен (опционально)

Если хотите использовать свой домен:

1. В Railway Dashboard → Settings → Domains
2. Нажмите "Add Domain"
3. Введите ваш домен (например, `seligertinder.ru`)
4. Настройте DNS записи у вашего регистратора:
   - Тип: `CNAME`
   - Имя: `@` или `www`
   - Значение: `web-production-4bcda.up.railway.app` (Railway покажет точное значение)
5. Обновите `WEB_APP_URL` в переменных окружения

## ✅ Проверка

После настройки проверьте:
- Приложение доступно по адресу: `https://web-production-4bcda.up.railway.app`
- Бот работает и может открыть Web App
- API отвечает: `https://web-production-4bcda.up.railway.app/api/users`

