# Настройка Worker процесса для Telegram бота в Railway

## ⚠️ ВАЖНО: Worker процесс должен быть запущен отдельно!

Telegram бот работает в отдельном процессе (worker), который должен быть запущен в Railway.

## 🔧 Как включить Worker процесс:

### Вариант 1: Через отдельный Service (рекомендуется)

Railway автоматически читает `Procfile` и создает процессы. Но если worker не запускается:

1. Откройте Railway Dashboard → Ваш проект
2. Нажмите **"+ New"** → **"Empty Service"** (или **"GitHub Repo"** если еще не подключен)
3. Подключите тот же репозиторий
4. В настройках нового сервиса:
   - **Name**: `worker` (или `bot`)
   - **Root Directory**: `/` (корень проекта)
   - **Start Command**: `cd python_backend && python3.12 bot.py || python bot.py`
   - Или оставьте пустым, если Railway автоматически читает Procfile

### Вариант 2: Через Settings → Deploy (если есть Custom Start Command)

1. Откройте Railway Dashboard → Ваш проект
2. Перейдите в **Settings** → **Deploy**
3. Найдите **"Custom Start Command"** или **"Start Command"**
4. Оставьте пустым (Railway должен использовать Procfile автоматически)

### Вариант 3: Проверьте, что Procfile правильно настроен

Railway должен автоматически запускать все процессы из `Procfile`:
- `web` - запускается автоматически
- `worker` - должен запускаться автоматически

Если worker не запускается, возможно нужно создать отдельный Service.

## ✅ Проверка, что Worker запущен:

1. Откройте Railway Dashboard → **Deployments**
2. Выберите последний деплой
3. Перейдите в **Logs**
4. Выберите процесс **`worker`** (не `web`!)
5. Должны увидеть:
   ```
   🤖 ЗАПУСК TELEGRAM BOT
   ✅ Application создан
   ✅ Бот запущен и готов к работе!
   ⏳ Ожидание обновлений от Telegram...
   ```

## 🔍 Если Worker не запускается:

1. **Проверьте переменные окружения:**
   - `BOT_TOKEN` должен быть установлен
   - `WEB_APP_URL` должен быть установлен

2. **Проверьте логи Worker процесса:**
   - Railway → Deployments → Logs → worker
   - Ищите ошибки при запуске

3. **Проверьте Procfile:**
   - Должна быть строка: `worker: cd python_backend && python3.12 bot.py || python bot.py`

## 📝 Примечание:

- **Web процесс** - это FastAPI сервер (основное приложение)
- **Worker процесс** - это Telegram бот (отдельный процесс)
- Оба процесса должны быть запущены одновременно!

